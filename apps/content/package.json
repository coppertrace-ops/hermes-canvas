{
  "name": "@hermes/content",
  "version": "0.0.0",
  "private": true,
  "description": "Sandbox content-origin app that serves artifact HTML from a separate origin. OWNED BY WARDEN — ATLAS-seeded placeholder.",
  "type": "module",
  "scripts": {
    "dev": "next dev --port 3001",
    "build": "next build",
    "start": "next start --port 3001",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .next .turbo out"
  },
  "dependencies": {
    "next": "15.1.6",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3"
  }
}
