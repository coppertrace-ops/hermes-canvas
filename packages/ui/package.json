{
  "name": "@hermes/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI primitives (design system, canvas panes chrome). OWNED BY GLASS — ATLAS-seeded boundary stub.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  }
}
