{
  "name": "@hermes/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Hermes Canvas design system — tokens, theming, and component primitives. OWNED BY GLASS (plan §7).",
  "type": "module",
  "sideEffects": [
    "*.css"
  ],
  "exports": {
    ".": "./src/index.ts",
    "./styles.css": "./src/styles.css",
    "./tokens": "./src/tokens.ts",
    "./theme": "./src/theme.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.7",
    "react": "19.0.0"
  }
}
