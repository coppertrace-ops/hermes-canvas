/**
 * @hermes/ui — shared UI primitives.
 *
 * OWNERSHIP: GLASS (plan §7). ATLAS created this boundary only. GLASS implements the
 * design-system components, the split chat/canvas layout chrome, and the pane
 * primitives here. Consumers import from the package name so the surface is stable.
 */

export const UI_VERSION = "0.0.0-pre-g3" as const;
