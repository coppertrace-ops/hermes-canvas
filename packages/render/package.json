{
  "name": "@hermes/render",
  "version": "0.1.0",
  "private": true,
  "description": "Canvas artifact renderers + shell primitives — safe Markdown (no raw HTML), strict Mermaid, resizable split pane, changed badge, artifact empty/loading/error states, and the canvas data-adapter interface. OWNED BY PANES (plan §7). Consumes @hermes/contract, @hermes/ui.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@hermes/contract": "workspace:*",
    "@hermes/ui": "workspace:*",
    "mermaid": "^11.4.1"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "jsdom": "^25.0.1",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  }
}
