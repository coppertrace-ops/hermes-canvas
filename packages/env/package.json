{
  "name": "@hermes/env",
  "version": "0.0.0",
  "private": true,
  "description": "Environment schema + validation for Hermes Canvas (ATLAS-owned). No secrets — schema only.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./client": "./src/client.ts",
    "./convex": "./src/convex.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
