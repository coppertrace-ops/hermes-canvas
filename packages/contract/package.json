{
  "name": "@hermes/contract",
  "version": "0.0.0",
  "private": true,
  "description": "Canvas API contract: zod schemas, shared types, tool manifest source of truth. OWNED BY LEDGER — this is an ATLAS-seeded boundary stub.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "gen:agent-api": "tsx scripts/gen-agent-api.ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
