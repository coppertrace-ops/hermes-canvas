# pnpm configuration
# Convex, Next.js and their peers behave best with hoisted binaries.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
# Keep the lockfile authoritative in CI.
prefer-frozen-lockfile=true
