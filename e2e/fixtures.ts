/**
 * @hermes/e2e — shared fixtures placeholder.
 *
 * OWNERSHIP: PROOF (security e2e -> WARDEN), plan §7. ATLAS created this boundary
 * only. PROOF wires Playwright, the auth/session fixtures, and the golden flows.
 */

export const E2E_PLACEHOLDER = true as const;
