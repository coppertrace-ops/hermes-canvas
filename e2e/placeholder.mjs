// Phase-0 placeholder. PROOF owns the real Playwright suite (security e2e -> WARDEN).
// Kept as a no-op so `pnpm test:e2e` is green before the runner is wired.
console.log("[e2e] placeholder — no specs yet (owned by PROOF).");
